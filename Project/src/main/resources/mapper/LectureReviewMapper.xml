<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lectureReview">

	<select id="getList" parameterType="lectureReviewDto" resultType="lectureReviewDto">
      SELECT *
      FROM
         (SELECT result1.*, ROWNUM AS rnum
         FROM
            (SELECT num, writer, content, target_id, ref_group,
               comment_group, deleted, LECTURE_REVIEW.regdate, profile, starRating
            FROM LECTURE_REVIEW
            INNER JOIN users
            ON LECTURE_REVIEW.writer=users.id
            WHERE ref_group=#{ref_group}
            ORDER BY comment_group ASC, num ASC) result1)
      WHERE rnum BETWEEN #{startRowNum} AND #{endRowNum}   
   </select>   
   <!-- 댓글은 실제로 삭제하지 않고 deleted 칼럼에 저장된값을 no 에서 yes 로 수정하는 작업만 한다. -->
   <update id="delete" parameterType="int">
      UPDATE LECTURE_REVIEW
      SET deleted='yes'
      WHERE num=#{num}
   </update>
   <!-- 댓글을 저장할때 댓글의 글번호도 미리 QnaFreeAnswerDto 객체에 담아와야 한다. -->
   <insert id="insert" parameterType="lectureReviewDto">
       INSERT INTO LECTURE_REVIEW
      (num, writer, content, target_id, ref_group, comment_group, regdate)
      VALUES(#{num}, #{writer}, #{content}, #{target_id}, #{ref_group},#{starRating},
         #{comment_group}, SYSDATE)
   </insert>
   <!-- 저장 예정인 댓글의 글번호를 미리 얻어내기 -->
   <select id="getSequence" resultType="int">
      SELECT LECTURE_REVIEW_seq.NEXTVAL
      FROM DUAL
   </select>
   <update id="update" parameterType="lectureReviewDto">
      UPDATE LECTURE_REVIEW
      SET content=#{content}, starRating=#{starRating}
      WHERE num=#{num}
   </update>
   <select id="getData" parameterType="int" resultType="lectureReviewDto">
      SELECT num,writer,content,ref_group,comment_group,deleted,regdate,starRating
      FROM LECTURE_REVIEW
      WHERE num=#{num}
   </select>
   <select id="getCount" parameterType="int" resultType="int">
      SELECT NVL(MAX(ROWNUM), 0)
      FROM LECTURE_REVIEW
   </select>   
</mapper>