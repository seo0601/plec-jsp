<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lectureStudent">

  <select id="getList" parameterType="lectureStudentDto" resultType="lectureStudentDto">
    	SELECT num, id, complete
    	FROM LECTURE_STUDENT  
  </select>   
  
  <insert id="lectureSignup">
	  INSERT INTO LECTURE
			(num, id, complete)
			VALUES(LECTURE_seq.NEXTVAL, #{id}, #{complete})
   </insert>
</mapper>